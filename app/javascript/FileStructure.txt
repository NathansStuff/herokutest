channels folder = default

packs = default, where index.jsx lives which is where the whole react front end is exported out of

routing = backend routing for different react components

pages !
    All the stuff related to the individual page goes here.
    
    Each webpage has its own folder.

    Each folder has a jsx file and a scss file.

components !
    All the reusable react components that are reused go here.

    Each component has its own folder.

    Each folder has a jsx and a scss file.